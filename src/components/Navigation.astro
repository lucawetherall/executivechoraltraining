---
const base = import.meta.env.BASE_URL;
const fullPath = Astro.url.pathname;
// Strip base prefix so active-link comparison works on GitHub Pages (base: '/executivechoraltraining')
const currentPath = base.length > 1
  ? fullPath.replace(base.endsWith('/') ? base.slice(0, -1) : base, '') || '/'
  : fullPath;

const navLinks = [
  { href: '/what-we-do/', label: 'What We Do' },
  { href: '/packages/', label: 'Packages' },
  { href: '/about/', label: 'About' },
  { href: '/insights/', label: 'Insights' },
];
---

<header class="nav" id="nav" role="banner" data-scrolled="false">
  <a href={base} class="nav__logo" aria-label="ECCT Home">ECCT</a>

  <nav class="nav__links" id="nav-links" aria-label="Main navigation">
    {navLinks.map(link => (
      <a
        href={base + link.href.slice(1)}
        class:list={['nav__link', { 'nav__link--active': currentPath === link.href }]}
      >
        {link.label}
      </a>
    ))}
  </nav>

  <a href={base + 'contact/#booking'} class="btn btn--nav nav__cta">Book a Session</a>

  <button class="nav__hamburger" id="nav-toggle" aria-label="Toggle menu" aria-expanded="false">
    <span></span>
    <span></span>
    <span></span>
  </button>
</header>

<!-- Mobile menu overlay — outside nav so it doesn't inherit mix-blend-mode -->
<div class="nav__mobile-menu" id="mobile-menu" aria-hidden="true">
  <nav aria-label="Mobile navigation">
    <a href={base} class="nav__mobile-link">Home</a>
    {navLinks.map(link => (
      <a href={base + link.href.slice(1)} class="nav__mobile-link">{link.label}</a>
    ))}
    <a href={base + 'contact/'} class="nav__mobile-link">Contact</a>
  </nav>
  <a href={base + 'contact/#booking'} class="nav__mobile-cta">Book a Session →</a>
</div>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    mix-blend-mode: difference;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 28px 48px;
    background: transparent;
    pointer-events: none;
    transition: padding 400ms var(--ease-out), background-color 400ms ease;
  }

  .nav[data-scrolled="true"] {
    mix-blend-mode: normal;
    background: rgba(23, 30, 25, 0.92);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    padding: 16px 48px;
    border-bottom: 0.5px solid rgba(122, 31, 46, 0.15);
  }

  /* Re-enable pointer events on interactive children */
  .nav > * {
    pointer-events: auto;
  }

  .nav__logo {
    font-family: var(--font-display);
    font-size: 1.375rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--white);
    text-decoration: none;
    line-height: 1;
  }

  .nav__links {
    display: none;
    gap: 2.5rem;
    align-items: center;
  }

  .nav__link {
    font-family: var(--font-sans);
    font-size: 0.6875rem;
    font-weight: 400;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--white);
    text-decoration: none;
    transition: opacity 300ms ease;
  }

  .nav__link:hover,
  .nav__link--active {
    opacity: 0.6;
  }

  .nav__cta {
    display: none;
    font-size: 0.6875rem;
    letter-spacing: 0.18em;
  }

  /* Hamburger */
  .nav__hamburger {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    background: none;
    border: none;
    cursor: pointer;
    padding: 0.5rem;
  }

  .nav__hamburger span {
    display: block;
    width: 22px;
    height: 0.5px;
    background-color: var(--white);
    transition: all 300ms ease;
  }

  .nav__hamburger[aria-expanded="true"] span:nth-child(1) {
    transform: rotate(45deg) translate(4px, 4px);
  }

  .nav__hamburger[aria-expanded="true"] span:nth-child(2) {
    opacity: 0;
  }

  .nav__hamburger[aria-expanded="true"] span:nth-child(3) {
    transform: rotate(-45deg) translate(4px, -4px);
  }

  /* Mobile menu overlay */
  .nav__mobile-menu {
    position: fixed;
    inset: 0;
    z-index: 999;
    background-color: var(--navy);
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 6rem 2rem 4rem;
    opacity: 0;
    pointer-events: none;
    transition: opacity 400ms var(--ease-out);
  }

  .nav__mobile-menu.open {
    opacity: 1;
    pointer-events: auto;
  }

  .nav__mobile-link {
    display: block;
    font-family: var(--font-display);
    font-size: 3rem;
    letter-spacing: -0.02em;
    text-transform: uppercase;
    color: var(--white);
    padding: 0.5rem 0;
    line-height: 1;
    border-bottom: 0.5px solid rgba(122, 31, 46, 0.2);
    transition: color 300ms ease;
  }

  .nav__mobile-link:hover {
    color: var(--sage);
  }

  .nav__mobile-cta {
    display: inline-block;
    margin-top: 2.5rem;
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--sage);
  }

  @media (min-width: 1024px) {
    .nav__links {
      display: flex;
    }

    .nav__cta {
      display: inline-block;
    }

    .nav__hamburger {
      display: none;
    }
  }

  @media (max-width: 767px) {
    .nav {
      padding: 24px 24px;
    }
  }
</style>

<script>
  // Scroll-aware nav
  const nav = document.getElementById('nav');
  window.addEventListener('scroll', () => {
    nav?.setAttribute('data-scrolled', window.scrollY > 60 ? 'true' : 'false');
  }, { passive: true });

  const toggle = document.getElementById('nav-toggle');
  const mobileMenu = document.getElementById('mobile-menu');

  toggle?.addEventListener('click', () => {
    const isOpen = toggle.getAttribute('aria-expanded') === 'true';
    const opening = !isOpen;
    toggle.setAttribute('aria-expanded', String(opening));
    mobileMenu?.classList.toggle('open', opening);
    mobileMenu?.setAttribute('aria-hidden', String(!opening));
  });

  // Close mobile menu on link click
  mobileMenu?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', () => {
      toggle?.setAttribute('aria-expanded', 'false');
      mobileMenu.classList.remove('open');
      mobileMenu.setAttribute('aria-hidden', 'true');
    });
  });
</script>
